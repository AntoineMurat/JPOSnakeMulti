/**
 * Classe représentant un bloc de corps de serpent.
 */
class BodyPart extends Drawable {

    /**
     * Constructeur permettant d'obtenir un bloc 
     * à partir de ses coordonnées.
     * 
     * @param  {Coordinates} les coordonnées initiales.
     */
    constructor(coordinates) {
        super(coordinates)
    }
}